<ion-view view-title="Result">
  <ion-content>
    <div class="list card">

      <div class="item item-avatar">
        <h1>{{details.title}}</h1>
        <h2>{{details.price}}</h2>
      </div>

      <div class="item item-body">
        <img class="full-image" src={{details.imageLink}}>
        <p>
         {{details.description}}
        </p>
        <p>
        <a class="button button-block button-balanced" href="{{details.link}}">
  		Go-To-Link	
		</a>
	
        <button class="button button-block button-energized ng-hide" ng-hide="{{details.itemSaved}}" ng-click="saveListing({{details.id}})">
  		Save
		</button>
	<button class="button button-block button-assertive ng-hide" ng-hide="!{{details.itemSaved}}" ng-click="saveListing({{details.id}})">
  		Remove
		</button>
        </p>
      </div>

    </div>
  </ion-content>
</ion-view>
